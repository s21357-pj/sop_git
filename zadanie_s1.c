#include <signal.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <unistd.h>
#include <sys/errno.h>

	int main(int argc, char **argv)
	{
	    if (argc > 1) {
		printf("%s\n", count, argv[1]);

    }
ergc; count++)
		{
				  printf("argv[%d] = %s\n", count, argv[count]);
				  	}
    }§:xit(EXIT_SUCCESS);
}
